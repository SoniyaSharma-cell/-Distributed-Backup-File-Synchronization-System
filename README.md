# -Distributed-Backup-File-Synchronization-System
Automated File Sync &amp; Versioning System is a Python &amp; Flask app for local version control. It uses SHA-256 hashing to detect file changes, saving space by backing up (v1, v2) only modified files to an SQLite database. Features a Bootstrap web dashboard with 1-click restore for disaster recovery.
